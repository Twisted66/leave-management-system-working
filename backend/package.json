{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "npm@10.5.0",
  "scripts": {
    "build": "cd ../frontend && rm -rf node_modules && rm -rf dist && rm -rf .vite && bun install --no-cache && vite build --outDir=../backend/frontend/dist --emptyOutDir"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "encore.dev": "^1.49.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}