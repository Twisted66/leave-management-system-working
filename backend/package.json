{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && npm install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "encore.dev": "^1.48.13",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
